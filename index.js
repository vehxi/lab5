// Входные данные: температура в градусах Цельсия
const celsius = 25

console.log("Температура в Цельсиях:", celsius)

// Формула пересчета
const fahrenheit = (celsius * 9/5) + 32
const kelvin = celsius + 273.15

console.log(`Результат: ${celsius}°C равно ${fahrenheit}°F`)